# docker-compose.yml
version: '3.8'

services:
  findmystore-dev:
    build: 
      context: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    restart: always
    
    # 🚨 .env 파일을 로드하여 Client ID를 주입합니다. (보안 유지)
    env_file:
      - .env